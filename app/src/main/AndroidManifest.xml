<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.quizapptask"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.QuizAppTask">


        <activity
            android:name=".ResultActivity"
            android:exported="true" />

        <!-- The QuizQuestionActivity is also launched internally. -->
        <activity
            android:name=".QuizQuestionActivity"
            android:exported="true" />

        <!-- MainActivity is the launcher and has an intent filter, so it must be exported. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
